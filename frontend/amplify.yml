version: 1

frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site deployment"
    build:
      commands:
        - echo "Build complete"

  artifacts:
    baseDirectory: /
    files:
      - index.html
      - style.css
      - script.js
      - proxy.py

  cache:
    paths: []

appRoot: /
